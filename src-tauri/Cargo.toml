[package]
name = "push-deck"
version = "0.1.0"
edition = "2021"
rust-version = "1.81"
build = "build.rs"

[lib]
name = "push_deck"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
tauri = { version = "2.10.3" }

[build-dependencies]
tauri-build = { version = "2.5.6" }
